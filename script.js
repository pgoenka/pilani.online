// script.js for pilani.online
// Currently minimal JavaScript is needed as the core interactions are handled via CSS

document.addEventListener('DOMContentLoaded', () => {
    // Optional: Add intersection observer to only load iframes when they enter viewport if native lazy loading is not sufficient
    // Since we used loading="lazy" on iframes, modern browsers handle this natively.
    console.log("pilani.online loaded successfully.");
});
